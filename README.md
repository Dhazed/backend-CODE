# Backend in Express.js progetto CODE

Web API made with Node and Express.

To install packages dependecies, run 

`npm i`

To start, run 

`npm start`

To start in dev environment, run 

`npm run dev`

GitHub
git checkout main
git pull
git checkout "MY BRANCH"
git merge main